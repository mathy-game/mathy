import { _decorator, Color, Component, Graphics, Node } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('FunctionsBG')
export class FunctionsBG extends Graphics {
    start() {
        // this.moveTo(0, 0)
        // this.node.setScale(250, 250)
        // this.strokeColor = Color.WHITE
        // this.lineWidth = 5 / 250
        // for (
        //     let x = 0;
        //     x <= 100;
        //     x += 1 / 250
        //   ) {
        //     const value = Math.sin(x)
        //     this.lineTo(x, value)
        //   }
        // this.stroke()
        // console.log('Hello cocos creator!')
    }

    update(deltaTime: number) {
    }
}


